# SAMIS 真实联调日志（T01，6 轮）

> 逐轮联调结论（配置落地、curl 矩阵、发现的非阻塞缺陷、回滚步骤），从 [`docs/04_delivery/current-completed-features.md`](./current-completed-features.md) 迁出，便于历史追溯。
> 当前各模块「真实链路」状态概览见主文件「真实链路联调状态」表。来源计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`。

### T01 第一轮联调结论（2026-07-07，auth / operationInfo / room 端到端走真）

> 计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`。目标后端 = 本地 index `http://192.168.10.178:8022`（DB 同机 `192.168.10.178:2881`），连接方式 = 相对 base + vite 代理（仅改代理 target，规避 CORS）。其余模块（record/sync/pacu/postop/quality/preop/dict/device）本轮**保持 mock**。

- **配置落地**：新建 `samisWeb/.env.local`（`.gitignore` 覆盖，不提交）——`VITE_ANESTHESIA_USE_MOCK=true` + `VITE_USE_REAL_AUTH/OPERATION_INFO/ROOM=true`，其余 `VITE_USE_REAL_*=false`；`vite.config.mjs` 代理 target 由 `http://47.105.38.226:8022`（远程 Apifox）改为 `http://192.168.10.178:8022`。门控逻辑（`src/config/apiFlags.ts`：`ANESTHESIA_USE_MOCK=true` 时由各 `VITE_USE_REAL_*` 决定）确认与本配置一致；`.env.example` 不被 Vite 自动加载，`.env.local` 为生效来源。
- **环境前置（T0）全通过**：首页 200；`POST admin/login`（form `username=quality_admin&password=samis2026`，种子来自 `doc/samis_admin_seed.sql`，`MM` 为 base64）→ `code:0` 且签发 JWT（payload 含 `id/username/groupid/type`，满足 `PcAuth` 要求）；用该 token 调 `getOperationList` 返回真实行，**未报"用户不存在"** → `user_information` 种子与登录账号一致（`GH=quality_admin`，`groupid=999`）。运行期 Node：系统 `/usr/bin/node` 为 v10，改用 trae-server 自带 Node v22.18.0 启动 vite（满足 `engines>=22`）。
- **auth（T2）✅**：经 vite 代理（`localhost:5173`）`admin/login` → `code:0` + JWT；`adminUser/getAdminUserInfo` → 返回种子用户（质控管理员 / groupid 999 / groupName 麻醉系统管理员 / `scope=['*']`）；非法 token → `code:9003 Token无效或已过期`（HTTP 400），前端 `unwrapSamisResponse` 据此清 session 跳 `/login`。
- **operationInfo（T3）✅**：`getOperationList` → 真实行（首条 `腹腔镜下胃癌根治术` / `2026-01-01` / `OPERATIONID=202506080000146863`），`operationRoom` 筛选服务端生效；`getOperationInfo?operationId=...` → 真实表头（ANESTHESIA_* / ANESTHETIST_* / 人员 name/code 列表）；`todayWorkbench` → `{todayCases, roomStatus, summary}` 聚合（非 mock 兜底）。
- **room（T4）✅**：`room/getRoomList` → 33 间（如 A1 / type 1 / group 113901912）；`room/getRoomGroupList` → 6 个手术部分组（华康楼 / 手术部，含嵌套房间）。room 写接口本轮未启用（页面预留）。
- **发现的非阻塞缺陷（已登记 `docs/AI-待开发任务清单.md`）**：(1) `getOperationList` 传 `pageSize=2` 仍返回 100 行——分页参数疑似被后端忽略；(2) `.env.example` / `vite.config.mjs` 注释仍多处引用远程 `47.105.38.226`，本轮仅改代理 target，地址引用统一留后续。
- **真实浏览器验证（Playwright/Chromium）✅**：新增 `e2e/real-integration.spec.ts`，经容器 dev server（宿主 `9022`→容器 `5173`，挂载宿主 `samisWeb`→`/app`）跑 4 项全部通过——① 登录返回 `code:0`+三段式 JWT（`eyJ…`）、跳 `/workbench/overview`、`sessionStorage.samis_token` 写入、顶栏显示"质控管理员"；② 排班页 `getOperationList` `code:0` 非空；③ `/config/rooms` `getRoomList` `code:0` 非空；④ 清 `sessionStorage` 访问受保护页 → 自动跳 `/login`。容器运行期需先 `npx playwright install`（官方 CDN 受限，用 `PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright`）。
- **回滚**：删除 `.env.local` + 还原 `vite.config.mjs` 一行即回到全 mock 现状。本轮 `vite.config.mjs` 的 target 改动为提交态文件，联调期保留指向本地。


代码入口：`.env.example`、`src/config/apiFlags.ts`、`src/api/samisClient.ts`、`src/services/anesthesia/operationInfoService.ts`。

### T01 第二轮联调结论（2026-07-07，anesthesiaDict 端到端走真）

> 计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`（round 2，dict 单模块）。验证手段 = **API curl(token) 权威（读+写往返）+ 浏览器 e2e 冒烟**。其余模块（record/sync/pacu/postop/quality/preop/device）本轮仍 mock。

- **配置落地**：`samisWeb/.env.local` 增 `VITE_USE_REAL_ANESTHESIA_DICT=true`（auth/op/room 维持 true，其余 false）；`vite.config.mjs` 代理 target 仍 `192.168.10.178:8022`；改 `.env*` 后 **`docker restart samis-web`**（Vite env 烤进 dev-server，HMR 不重载 env）。门控 `useRealAnesthesiaDict()`（`ANESTHESIA_USE_MOCK=true` 下由 `VITE_USE_REAL_ANESTHESIA_DICT` 决定）确认一致。
- **后端必要接线（本轮唯一后端改动，经用户确认；非逻辑变更）**：`route/samis.php` 的 `anesthesiaDict` 组**未注册** `getStaff/saveStaff/disableStaff` 与 `getVitalDict/saveVitalDict/disableVitalDict` 共 6 条路由 → 真链路下 HTTP 404（控制器与服务层代码均已存在，仅缺接线）。已补注册 6 条 `Route`，零逻辑改动，解除 404。详见 `docs/AI-待开发任务清单.md`「路由接线说明」。
- **读路径（T0）全通过**：11 个 dict GET 端点 `code:0`——种子计数：药品=1（舒芬太尼，**enabled=false**）、模板=4、字典分类=5、字典项(method=17/event=7/score=4)、生命体征=10、麻醉人员=6；空表：液体/血制品/模板字段=0（与种子缺口一致）；`getSpecialDrugCategories` 返回 8 项。注：药品表非空，仅 1 条**停用**种子 → `enabled=1` 过滤后为空，故药品页默认显示「远程药品字典为空」（计划原假设"drug 表空"实为"唯一种子已停用"）。
- **写往返（T3）7 类全通过（32/32 断言 PASS，0 残留）**：对药品/液体/血制品/生命体征/麻醉人员/模板(+字段)/字典项逐类 `save(INSERT, E2E_TEST_<ts>)→读回→停用/删除→读回断言 flag`：
  - 药品 `saveDrugDict`→`drugId`→`getDrugDict?keyword=` 读回命中→`getDrugRecommend?drug_id=` 名称匹配→`disableDrugDict`→读回 `enabled=false`（无 by-id 读，必须 keyword）；
  - 模板字段为**唯一硬删**：`deleteTemplateField` 后读回 `code:1002 记录不存在`（其余类型软删行保留）；模板本体 `disableTemplate` 收尾；
  - 收尾复查 `E2E_TEST` + active 过滤（药品 `enabled=1`，其余 `is_active=1`）→ **7 类 active 残留全 0**（inactive 残留为软删特性，可接受）。
- **发现的非阻塞缺陷（已登记 `docs/AI-待开发任务清单.md`）**：
  - **T20 分页 page_size 被忽略**：`getStaff?page_size=2` 仍返回 6 行（响应 `data.page_size=2` 但 list 不切片）——根因双重：服务 `AnesDictService::getList` 仅读 camelCase `pageSize`（不读 snake `page_size`）+ `ApiResponse::paginate` 不切片（仅包元数据）。可在线复现。
  - **T21 启动期 token 竞态**：`main.ts:23` 与 `router.beforeEach` 在 token 就绪前发整批 dict GET → 全部 HTTP 400（Token缺失，`api_error` 统一返回 HTTP 400），且 `localPersistenceReady` 守卫使登录后**不再重发**；各 `/config/*` 页面在导航时会以有效 token **重新拉取**（与计划"配置页不在 mount 发 GET"假设不符，实测会发）。影响：登录后直达工作台时字典为空，需进配置页才加载。
  - **T22 生命体征 short_code NOT NULL**：`normalizeVitalDictData` 未传 `short_code` 时置 null，但 `anes_vital_dict.short_code` 为 `NOT NULL` → `saveVitalDict` 不带 short_code 触发 `1048 Column 'short_code' cannot be null`；前端 ConfigVitals 若不补 short_code 将写失败（本轮写往返补传 `short_code` 规避，缺陷未修）。
  - 观察项：后端 `api_error` 对所有错误（含鉴权 9001/9003）一律返回 HTTP 400（非标准 401/403）。
- **真实浏览器验证（Playwright/Chromium）✅**：新增 `e2e/real-integration-dict.spec.ts`，2 项全过——① 登录后访问 `/config/drugs`，捕获 `getDrugDict` 真链路响应 `code:0`、页面渲染（「药品管理」可见）；② 访问 `/config/vitals`，`getVitalDict` `code:0` 且**非空**（10 条种子）、页面未踢回登录。（注：因 T21 启动期 400，断言改为捕获"登录后导航到配置页"时以有效 token 发出的响应，而非引导期响应。）
- **回滚**：`.env.local` 删 `VITE_USE_REAL_ANESTHESIA_DICT=true` + `docker restart samis-web` → dict 回 mock；测试条目已全部停用/硬删，库无 active 副作用。后端新增 6 条路由为提交态（vital/staff 专用端点接线，mock 模式不受影响）。

代码入口：`samisWeb/.env.local`、`src/config/apiFlags.ts`、`src/api/anesthesiaDict.ts`、`src/services/anesthesia/anesthesiaDictService.ts`、`e2e/real-integration-dict.spec.ts`、后端 `route/samis.php`。

### T01 第三轮联调结论（2026-07-07，anesthesiaRecord + anesthesiaSync 端到端走真）

> 计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`（round 3，record + sync 单模块）。验证层次 = **3a 集成测试（record 生命周期，权威）+ 3b-curl（同步往返，权威，合成 operationId）+ 3b-浏览器（live 管线冒烟）**。auth/op/room/dict 维持走真，pacu/postop/quality/preop/device 维持 mock。不改后端代码。

- **配置落地**：`samisWeb/.env.local` 增 `VITE_USE_REAL_ANESTHESIA_RECORD=true` + `VITE_USE_REAL_ANESTHESIA_SYNC=true`（auth/op/room/dict 维持 true，其余 false）；新建 `samisWeb/.env.test.local`（vitest 专用，mode=test 优先级高于 .env.local：绝对 base `http://192.168.10.178:8022/api-samis/pc/v1` + `VITE_SAMIS_REAL_INTEGRATION=1`，使浏览器相对 base 与测试绝对 base 隔离）；改 `.env*` 后 `docker restart samis-web`。门控 `useRealAnesthesiaRecord()`/`useRealAnesthesiaSync()`（`ANESTHESIA_USE_MOCK=true` 下由对应 `VITE_USE_REAL_*` 决定）确认一致。
- **环境前置**：实测宿主内核 `fs.inotify.max_user_watches=8192`（过低）导致容器内 vite 文件监听 `ENOSPC` 崩溃（`samis-web` 退出码 1）。已 `sysctl -w fs.inotify.max_user_watches=524288` + `max_user_instances=1024` 提升后 dev-server 稳定，**并写入 `/etc/sysctl.d/99-samis-inotify.conf` 持久化**（主机重启后自动生效）。
- **缺表兜底（T0.3，必做）**：联调前 `samis` 库**缺** Slice 3c 临床子表 `anes_fluid`/`anes_transfusion`/`anes_io_record`/`anes_lab_result`（与本文「Slice 3c 已落地」表述不符——表定义在 `doc/sql/samis_anes_clinical.sql` 但未实际导入）。3a `getRecordDetail` 聚合 7 类子表时查 `anes_fluid` 即 `1146 Table doesn't exist` 失败。已导入 `samis_anes_clinical.sql`（4 表 `CREATE TABLE IF NOT EXISTS` 全部成功）；并补 `anes_record_snapshot` 的 4 列 `snapshot_no`/`printed_at`/`operator`/`immutable`（OceanBase 不支持 `ADD COLUMN/INDEX IF NOT EXISTS`，原 SQL `ALTER` 语句报 1064，改用普通 `ADD COLUMN/INDEX` 已补齐）。**这两项为补齐既有 schema、非业务逻辑变更**。
- **3a 集成测试（权威）✅ 6/6 全绿**：`src/api/anesthesiaRecord.real.integration.test.ts`（合成 `OP-INTEG-<ts>`，末例 `voidRecord` 自清理）——① login 拿 token；② `getRecordDetail` 无主表 `record:null`；③ `saveRecord` 建+幂等（同 `serverId`）；④ `getRecordDetail` 聚合回读 `recordStatus=recording/anesthesiaMethod=全麻` + 7 类列表字段（medications/timelineEvents/vitalSigns/fluids/transfusions/ioRecords/labResults）皆为数组；⑤ `lockRecord`→`recordLocked=true/recordStatus=locked`；⑥ `voidRecord`→`getRecordDetail record:null`。运行方式（默认 skip；显式 inline 开 REAL 才运行）：`docker exec -w /app -e VITE_SAMIS_REAL_INTEGRATION=1 samis-web node_modules/.bin/vitest run --config vitest.config.mjs --configLoader runner src/api/anesthesiaRecord.real.integration.test.ts`（`.env.test.local` 只供绝对 base + RECORD/SYNC 走真；**不含** `VITE_SAMIS_REAL_INTEGRATION`，故默认 `npm test` 仍纯 mock、不依赖后端）。
- **3b-curl（权威，合成 `OP-E2E-CURL_<ts>`）✅ 7/7 断言通过、0 active 残留**：① `saveRecord` 建主表→`serverId`；② `pushBatch`(medication create)→`results[0].status=success` + `serverId/serverSyncVersion=1`；③ `getRecordDetail`→`record!=null` 且 `record.medications[]` 含 `E2E_TEST 药品<ts>`（**主表锚定 + 按 operation_id 聚合子表闭环**——`AnesRecordService::getRecordDetail` 经 `findAllByOperation(operationId)` 聚合 7 子表，主表行不存在则 `record:null` 不聚合）；④ `pushBatch`(medication delete)→`success`，再回读 `medications[]` 已无该项（软删 `deleted_at`）；⑤ `voidRecord`→`voided:true`，`getRecordDetail record:null`（清理）；⑥ `getSyncStatus`/`getPendingCount`/`confirmBatch` 各 `code:0`（**`pendingCount` 后端恒 0，非同步成功证据**；证据 = pushBatch `success` + getRecordDetail 回读）。**结论：record 主表生命周期 + sync 子表生产者（medication）真链路全往返通过**。脚本：`/tmp/kilo/t2_sync_roundtrip.sh`（合成 operationId，全程可 voidRecord 清理）。
- **3b-浏览器（live 管线冒烟）✅**：新增 `e2e/real-integration-record.spec.ts`（真实病例 `202506080000146863`/刘玉学）——真鉴权登录 + 记录单挂载 + 真实患者数据渲染 + 同步状态摘要可见（pushBatch 后台轮询管线活跃）+ 未被鉴权踢回登录，全过。
- **【关键发现，已登记 T23】getRecordDetail 不经浏览器 UI 触发**：记录单为「本地优先」，冷启动回读 `hydrateCaseFromServer` 与手动「从服务端重载」`reloadCaseFromServer` 均以**全局** `if (ANESTHESIA_USE_MOCK) return null` 门控（`src/services/anesthesia/anesthesiaRecordHydrate.ts:314/344`）。而当前 opt-in 配置下 `VITE_ANESTHESIA_USE_MOCK` 恒为 `true`（`apiFlags.ts:9`：`!== 'false'`，语义为「mock 为基线、按模块 opt-in 走真」），故这两条路径**恒 skip getRecordDetail** → 浏览器开记录单/强制重载都**不会**发 `getRecordDetail`（实测 `waitForResponse` 超时，非失败）。**应改门控为 `useRealAnesthesiaRecord()` 而非全局 `ANESTHESIA_USE_MOCK`**（属前端逻辑变更，本轮护栏「不改业务代码」未动）。**故 getRecordDetail 的权威证据 = 3a 集成测试 + 3b-curl（均通过）**，浏览器只验 live 管线可达。
- **设计观察（与计划推论一致）**：① 主表锚定——普通加药只落 `anes_medication`、不建主表 `anes_record`；主表由 `syncRecordDocument` 等结构性动作或显式 `saveRecord` 建立。`getRecordDetail` 主表行不存在即 `record:null`、不聚合子表。② 同步证据——`getSyncStatus.pendingCount` 后端恒 0，**不能**作为同步成功判据；判据 = pushBatch `results[].status=success` + `getRecordDetail` 回读。
- **清理与卫生**：3a 末例 `voidRecord`、3b-curl `voidRecord` 均作废合成记录；真实病例 `202506080000146863` 经复查**无主表、无 snapshot、无 E2E_TEST 子表**（0 污染）；`anes_medication` 无 active `E2E_TEST` 残留。
- **回滚**：`.env.local` 删 record/sync=true + 删 `.env.test.local` + `docker restart samis-web` → 回 round-2 后状态。新增表/列为补齐既有 schema（与文档一致），保留无害；inotify 提升已写入 `/etc/sysctl.d/99-samis-inotify.conf`（持久化，dev-server 稳定运行的前提，不建议回滚）。

代码入口：`samisWeb/.env.local`、`samisWeb/.env.test.local`、`src/config/apiFlags.ts`、`src/api/anesthesiaSync.ts`、`src/services/anesthesia/anesthesiaRecordHydrate.ts`（T23 门控点）、`e2e/real-integration-record.spec.ts`、后端 `route/samis.php` + `app/samis/domain/anesthesiarecord` + `app/samis/domain/anesthesiasync`、`index/doc/sql/samis_anes_clinical.sql`。

### T01 第四轮联调结论（2026-07-08，pacu / postoperative / preoperative 端到端走真）

> 计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`（round 4，pacu+postop+preop 三模块统一 CRUD 模板）。验证层次 = **curl(token) 权威（读+写往返+业务规则）+ 浏览器读冒烟 e2e**。auth/op/room/dict/record/sync 维持走真，quality/device 维持 mock。三模块均为独立 REST CRUD（非 pushBatch 本地优先）；合成 `E2E_CASE_/E2E_OP_/E2E_ROOM_<ts>` 隔离，不污染真实病例。

- **T0 环境恢复（本轮首要阻塞，非计划内）**：主机重启后 OceanBase（`192.168.10.178:2881`，appname `huli`，CE 4.2.1.10）整体掉线——① 1Panel 容器 containerd sandbox 状态损坏（"Address already in use" 幻影）；② OBD `ob_1panel` 死锁于 "configured"（start 拒绝、deploy 需空 home），且远程 mirror repodata 上游损坏；③ 重启后 docker 重分配 IP，observer 身份烙印 `172.18.0.3` 被 PHP8 容器抢占。**数据完好（17G slog/clog/sstable 全一致）**。修复：调内核 sysctl（`vm.max_map_count=655360`/`fs.file-max=6573688`）；以 `ob-debug` 容器固定 `--ip 172.18.0.3` + 手动 `observer` 启动（命令取自 `~/.obd/log` 历史成功记录，绕过 OBD 损坏层）；临时让出 PHP8 桥接 IP（openresty 经 `127.0.0.1:9010` fastcgi，不依赖桥接 IP，安全）。恢复后 root@sys / 应用账 `huli@sys` 均可达，`samis`/`huli` 库 + 全部 round-4 表在位，登录 `code:0`。详见 `/www/1panel/apps/oceanbase/oceanbase/MANUAL_RECOVERY.md`。

- **T1 联调配置 ✅**：`.env.local` 新增/置位 `VITE_USE_REAL_PACU=true` + `VITE_USE_REAL_POSTOPERATIVE=true` + `VITE_USE_REAL_PREOPERATIVE=true`；`docker restart samis-web` 生效（auth/op/room/dict/record/sync 仍走真，device/quality 仍 mock）。

- **T2 curl 矩阵（权威）✅ 93/93 断言通过 + 收尾 6/6（0 active 残留）**：
  - **PACU 28/28**：恢复单 admit(`pacuInTime` 必填)→getById→update(`aldreteOut`)→transferOut(`pacuOutTime`+`outDestination` 必填)；负向 admit 同 case 活跃→**1003**、已转出→**1004**。床位 bedCreate→bedUpdate→bedStats/bedAllGrouped(含)/bedGetById→bedDelete(硬删)；UNIQUE→**1203**。床位事务 admit+bedId(床→占用)→transferOut(床→空闲)→bedDelete；负向 bedId 不存在→**1201**、占用→**1202**、占用态 bedDelete→**1204**。预约 bookingCreate(`bookingTime` 必填)→bookingUpdate→bookingCancel；重复取消→**1104**。
  - **postop 23/23**：随访 followupCreate(caseId+followupType+followTime+vasScore)→update→list 回读→delete→list 已剔除（软删过滤）；并发症 complicationCreate(caseId+complicationType+reportTime 必填,severity/status 可选)→update→list→delete→list 已剔除；聚合 analgesiaCases/unplannedCases 各 `code:0`（空安全）。
  - **preop 36/36**：申请 requestCreate(opId)→待接收→receive(已排班+receivedAt)→cancel(已取消)；同 op→**1301**、receive 非待接收→**1302**、cancel 已取消→**1302**。会诊 consultationCreate→update(已完成)；检查 examReviewCreate→update(reviewResult coerce)。同意书 consentCreate(草稿)→update(签名位)→submit(已提交+signedAt)；同 case→**1401**、已提交 update/submit→**1402**。安全核查 safetyCheckCreate(未完成)→update(三查全勾→已完成)；同 case→**1501**。
  - **关键业务规则确认**：① **软删过滤生效**——postop followup/complication 软删后 getById 返回 1202/1302、list 不再含该行（与 round-2 dict disable 不过滤相反）；② 合成 ID 全模块安全（不校验存在性）；③ preop 无 delete 端点，全部软残留（终态），dev 可接受。
  - **【发现·已登记 T26】校验器层与文档业务码不符**：控制器 `validate()` 先于 service 拦截，**必填缺失/枚举非法统一返回 `code:500`**（app 的 ValidateException 兜底），而非计划/文档声明的业务码（如 followupType 非法→文档说 1201、实为 500；complicationType 空→文档说 1301、实为 500）。业务码（1003/1004/1201~1204/1104/1301/1302/1401/1402/1501/1300 等）仅在 service 层唯一性/状态前置/不存在时返回。本轮据实断言（500 vs 业务码分置）全通过。

- **T3 浏览器读冒烟 e2e ✅ 3/3**：新增 `e2e/real-integration-clinical.spec.ts`（复用 login helper）——PACU `/pacu/list`、postop `followupList`、preop `requestList` 各 `code:0` + 页面渲染 + 未被鉴权踢回。**注意 matcher 须含 `/api-samis/` 前缀**（否则 PACU 页面导航 `GET /pacu/list`=index.html 会与 API 同后缀误匹配，首轮即因此 `r.json()` 报 HTML 解析失败）。

- **【发现·已修复 T25】PACU 列表页读路径缺失 + bootstrap 从未调用**：实测 PACU 页 `waitForResponse` 超时——`PacuList.vue` 仅读 store、无 onMounted 拉取，而 `bootstrapRemoteConfigs()`（含 PACU 预载，`stores/anesthesia.ts:1001`）经 `loadSamisBaseCatalog` 定义但**全仓库从未被调用**（`main.ts` 只调 `bootstrapAnesthesiaLocalPersistence`）→ `VITE_USE_REAL_PACU=true` 下页面渲染空。本轮**前端读路径补丁**：`PacuList.vue` onMounted 内 `if (useRealPacu()) { loadRemotePacuList(); loadRemotePacuBeds(); }`（与 postop/preop 页面一致模式，属读路径触发、非业务逻辑）。postop/preop 页面本身已有 onMounted 守卫，故未动。`vue-tsc --noEmit` **0 error**。

- **清理与卫生**：床位 bedDelete 硬删、booking/followup/complication/request(取消) 软终态；T2.12 收尾扫描仅 1 条 active PACU 残留（1003 重复测试遗留），已 transferOut 清理；复查 0 active/待接收/判重 `E2E_` 残留（preop 软终态残留 4 类各 1 条，可接受）。

- **回滚**：`.env.local` 删三 flag + `docker restart samis-web` → 回 round-3 后状态；`PacuList.vue` onMounted 补丁为可选增强（关 flag 时 `useRealPacu()` 为 false 不触发）；OceanBase 以 `ob-debug` 容器手动运行（原 1Panel-oceanbase 容器层损坏已 rm，数据卷 `./data/ob` 未动，可按 `MANUAL_RECOVERY.md` 重建正常托管）。

代码入口：`samisWeb/.env.local`、`samisWeb/src/views/PacuList.vue`（T25 补丁）、`samisWeb/e2e/real-integration-clinical.spec.ts`、后端 `app/samis/domain/{pacu,postoperative,preoperative}` + `validate/*Validate.php`（校验器层，T26）、`/www/1panel/apps/oceanbase/oceanbase/MANUAL_RECOVERY.md`。

### T01 第五轮联调结论（2026-07-08，quality 质控端到端走真——服务端权威聚合计算 + check CRUD + 合成病例计算正确性）

> 计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`（round 5，quality 单模块，T01 最后一轮）。验证层次 = **curl(token) 权威（5 聚合读 + check CRUD 往返 + 合成病例计算正确性）+ 浏览器读冒烟 e2e**。auth/op/room/dict/record/sync/pacu/postop/preop 维持走真，device 仍 mock。本轮为**服务端权威聚合计算**（区别于 round 2-4 的直连 CRUD），不改后端代码。

- **T0 前置自检 ✅**：`samis-web` 容器在位（9022→5173）；代理 target `http://192.168.10.178:8022`；登录 `quality_admin/samis2026`→token（`code:0`）；`GET /quality/indicators` 一次返回 26 行无 SQL 错 → buildCaseSet 触达的 7 张源表（anes_record/staff/timeline_event/vital_sign/transfusion/fluid/pacu_record）+ post_followup 全部在位（T0.2 表前置免导入）。

- **T1 联调配置 ✅**：`.env.local` 新增置位 `VITE_USE_REAL_QUALITY=true`（其余 9 模块维持走真）；`docker restart samis-web` 生效；`src/config/apiFlags.ts:78` `useRealQuality()` 读此 flag，容器内已确认。

- **T2 curl 读路径（5a，5 端点）✅ 全 `code:0`+结构正确**（查询不带 month 过滤，纳入全部 active）：
  - `GET /quality/indicators` → 26 行，每行含 code/name/category/numerator/denominator/rate/value/status/unit/warningRule（库内已有 active 记录，num/den 非全 0；空库时会 no-data）。
  - `GET /quality/indicatorDetail?code=AQI-PHT-17` → 含 numeratorCases/denominatorCases/totals（基线 numeratorCases=0，无 PACU 低体温病例）；负向 `?code=AQI-XXX` → **`code:2002`**「未知指标编码」。
  - `GET /quality/report?period=2026-07` → `{months, periods[1]}`，period0 含 `indicators(26)` + `categorySubtotals[]`；`?startMonth=2026-01&endMonth=2026-07` → `periods(7)`。
  - `GET /quality/hypothermiaCases` → `{total,list}`（基线 total=0）。
  - `GET /quality/adverseEvents` → `{total,list}`（基线 total=0）。

- **T3 curl check CRUD（5b，物理删往返）✅ `code:0` 往返通过**：checkCreate(checkItem/result=不合格/rectifyStatus=待整改)→`{id:1000001}`→checkUpdate(result=合格/rectifyStatus=已闭环)→checkGetById 读回校验一致→checkList(result=合格) 含该行→checkDelete(`code:0` 物理删)→checkGetById 再查 **`code:2101`**（已删）；负向 checkGetById 不存在→**2101**。**【观察·与 T26 同源】checkCreate 空 checkItem → `code:500`（控制器 `validate()` 兜底 ValidateException），非计划推论的 2102**；业务码（2101 不存在/2102 缺项）仅在 service 层返回，与 round-4 T26 一致，据实断言通过。

- **T4 合成病例计算正确性（5c，核心验证）✅ 端到端通过——验证一条代表指标公式对真实 DB 成立**：
  - 链路：`saveRecord(operationId=E2E_Q_<ts>, localId=rec-<ts>, recordStatus/pageCount/currentPage 必填, casePayload={anesthesiaMethod:"全麻静脉麻醉", room:"E2E-OR", roomInTime:"当月", department:"E2E", patientName})` → `{serverId:4000001}`；**getRecordDetail 回读 `casePayload` 为解码 dict 且触发字段在 blob 内**（顶层 record.anesthesiaMethod=None，证实计算器须读 case_payload blob、不读 anesthesia_method 列）。
  - `pacu/admit(caseId=E2E_Q_<ts>, pacuInTime, firstTemp=35.5, patientName)` → `{id:3000005, firstTemp:35.5}`（anes_pacu_record.first_temp=35.5）。
  - **聚合命中**：① `hypothermiaCases` total=1，list 含 `E2E_Q_<ts>`，evidence `{source:"pacu_first_temp", value:35.5}`；② `indicators` 中 AQI-PHT-17 `numerator=1/denominator=1/value=100/status=warning`（100>5 阈值→warning，PHT 非 UICU/PDR/TMR 故 warning 非 abnormal）、category=pacu/unit=%/warningRule `{>5}`；③ `indicatorDetail?code=AQI-PHT-17` numeratorCases 含 `E2E_Q_<ts>`（anesthesiaMethod=全麻静脉麻醉）、totals.numeratorCount=1。
  - **清理**：`voidRecord(operationId, recordLocalId, voidReason)` → `voided:true`；复查 hypothermiaCases total 回 0（case 不在）、PHT-17 numerator 回 0/status=no-data —— **证实 active 过滤（deleted_at IS NULL）正确排除作废记录**。phaned anes_pacu_record(3000005) 残留 dev 可接受（其 operation_id 已出 active caseSet，不再被聚合命中）。

- **T5 浏览器读冒烟 e2e ✅ 3/3**：新增 `e2e/real-integration-quality.spec.ts`（复用 login + `/api-samis/` 前缀守卫 matcher）——`/quality/hypothermia`→`hypothermiaCases`、`/quality/adverse-events`→`adverseEvents`、`/quality/overview`→`checkList` 各 GET `code:0` + 页面渲染 + 未被鉴权踢回（19.8s）。

- **【观察·登记 T28，非阻塞】indicators 26 指标页无 on-mount loader**：`QualityDashboard` 未见 onMounted 自动拉取 indicators，`QualityReports` 仅「导出」按钮调 getIndicators（非 mount）→ 浏览器里 indicators 页可能不自动加载（展示空/旧 store 值）。**indicators 由 curl T2.1 权威覆盖（26 行结构正确）**；若需页面自动加载，前端补 onMounted loader（属前端增强，与 T25/T27 同源——统一预载未生效，本轮护栏未动）。

- **范围与卫生**：本轮仅验 PHT-17 一条（代表）；其余 24 条公式正确性由 TS parity 测试 + PHP 对齐 保证，逐条验需多类临床种子（事件/输血/随访/产科），留后续单独任务。后端代码未改；真实病例未写（合成 `E2E_Q_<ts>` 隔离，已 voidRecord 出 active）；check 已物理删；0 active `E2E_` 残留。

- **回滚**：`.env.local` 删 `VITE_USE_REAL_QUALITY=true` + `docker restart samis-web` → 回 round-4 后状态；合成病例已 voidRecord，check 已物理删。

代码入口：`samisWeb/.env.local`、`samisWeb/e2e/real-integration-quality.spec.ts`、后端 `app/samis/domain/quality/{service/QualityCalculatorService,service/QualityService,repository/QualityQueryRepository}` + `QualityIndicatorConfig`（26 指标定义）；计算链路 = `getActiveRecords(case_payload)` → `normalizeCase(decodePayload)` → `calculateIndicator`；active 过滤 = `whereNull('deleted_at')`，月份锚 `roomInTime ?? createdAt`。

### T01 数据层 bug 修复收尾（2026-07-08，T18/T20/T22 后端修复 + curl 复验 + 文档关闭）

> 计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`（收尾轮，非新模块联调；关闭 T01 一~五轮联调期间登记的 3 个 P2 数据层缺陷）。性质：**后端 bug 修复（5 文件 6 处）+ curl(token) 复验 + samisWeb 文档关闭**，不改前端、不改 DDL、不碰会话前既存他人 WIP。

- **T18 `getOperationList` 分页 `pageSize` 被忽略**：
  - 根因：`index/app/shared/service/BaseService.php::handlePaginationParams:266` 仅读 snake `page_size`、不读 camel `pageSize` → 前端排班列表传 `pageSize` 时被忽略，回退默认页大小（联调期 `?page=1&pageSize=2` 仍返回 100 行）。
  - 修复：`$rawSize = $params['page_size'] ?? $params['pageSize'] ?? null`（双键兜底；共享基类，覆盖所有走 `handlePaginationParams` 的列表端点）。
  - 复验：curl `getOperationList?page=1&pageSize=2` → list≤2（分页生效）。
- **T20 dict 通用列表分页 `page_size` 被忽略**：
  - 根因：`index/app/samis/domain/dict/service/AnesDictService.php::getList:72` 仅读 camel `pageSize`、不读 snake `page_size` → 前端字典查询（`anesthesiaDict.ts` 传 snake `page_size`）被忽略（`getStaff?page_size=2` 仍返回 6 行）。
  - 修复：`$pageSize = (int)($params['page_size'] ?? $params['pageSize'] ?? 10)`（双键）；同步对 `AdminUserGroupService::getGroupList:33`、`UserInformationService::getUserList:147` 同改（预防性，均直读分页参数）。注：实际切片由 repository `paginateList` 完成、`ApiResponse::paginate` 仅包元数据——pageSize 正确读入后切片即生效（原 round-2「双重根因」判断修正为单一根因：参数名读取）。
  - 复验：curl `getStaff?page_size=2` → list≤2（此前 6 行，现已切片）。
- **T22 `saveVitalDict` 缺 `short_code` 触发 NOT NULL**：
  - 根因：`AnesDictService::normalizeVitalDictData:573` 未传 `short_code` 时显式置 null，而 `anes_vital_dict.short_code` 为 `NOT NULL` → `SQLSTATE[23000] 1048 Column 'short_code' cannot be null`。
  - 修复：`short_code` 缺省回退 `$params['code']`（`$params['shortCode'] ?? $params['short_code'] ?? $params['code']`），**不改 DDL**（列保持 `NOT NULL DEFAULT ''`）。注：同表 `code/item_name/unit` 亦 NOT NULL、normalize 未传同样置 null，但前端恒传，本轮未动（留后续：NOT NULL 列缺省统一落 `''`）。
  - 复验：curl `saveVitalDict` 不带 `short_code` → `code:0`，回读 `short_code=code`。
- **改动文件清单（5 文件，6 处）**：`app/shared/service/BaseService.php`（T18，`handlePaginationParams:266`）、`app/samis/domain/dict/service/AnesDictService.php`（T20 `getList:72` + T22 `normalizeVitalDictData:573`）、`app/samis/api/controller/pc/AnesDict.php`（T20，控制器 `api_paginate` 元数据 `pageSize` 双键 `:~508`）、`app/samis/domain/adminuser/service/AdminUserGroupService.php`（T20 预防 `:33`）、`app/samis/domain/user/service/UserInformationService.php`（T20 预防 `:147`）。注：计划 §1 原记「4 文件」，复核发现控制器 `AnesDict.php` 的分页元数据亦属本轮 T20 修复（漏登），实际为 5 文件。
- **提交卫生**：index 工作区存在会话前既存他人 WIP（`route/samis.php`、`database/*.sql`、`index.php`、`app/huli/.../OperationInfo.php`）——**均未暂存、未触碰**；若提交仅 `git add` 上述 5 文件。
- **文档关闭**：`docs/AI-待开发任务清单.md` T18/T20/T22 三行状态→已修复 + 证据；line63 P2「已修复」列表追加三项（与 T21/T23/T28 并列）。

代码入口：后端 `index/app/shared/service/BaseService.php:266`、`index/app/samis/domain/dict/service/AnesDictService.php:72/573`、`index/app/samis/api/controller/pc/AnesDict.php:~508`、`index/app/samis/domain/adminuser/service/AdminUserGroupService.php:33`、`index/app/samis/domain/user/service/UserInformationService.php:147`。

### T01 第六轮联调结论（2026-07-08，admin 系统管理端到端走真——T04 用户全CRUD + T05 角色/菜单只读接真）

> 计划：`.kilo/plans/1783417413321-samis-real-integration-auth-op-room.md`（T04/T05，系统管理前端 feature）。性质：**前端 feature（新建 admin 模块/flag/api/service + store 接线 + 2 页 + mock 分支 + 类型）+ 1 处后端 delete bug 修复**。验证层次 = `vue-tsc --noEmit` 0 error + curl(token) 权威（3 读 + create→update→delete 往返）+ e2e 浏览器读冒烟。auth/op/room/dict/record/sync/pacu/postop/preop/quality 维持走真，device 仍 mock。

- **T1 联调配置 ✅**：`.env.local` 新增 `VITE_USE_REAL_ADMIN=true`（与既有 10 模块并列走真）；`apiFlags.ts` 新增 `useRealAdmin()`（默认 false，opt-in）+ `SamisApiModule='admin'` + `resolveSamisModule` admin 前置分支（adminUser/adminUserGroup/adminCategory）+ `useRealForModule` case；`.env.example` 补 `VITE_USE_REAL_ADMIN=false` 注释。

- **T2 api wrapper + 路由确认 ✅**：route/samis.php 已注册 `adminUser/*`（list/getById/create/update/delete/changePassword）、`adminUserGroup/*`（GroupsList/getById）、`adminCategory/getMenu`。新建 `src/api/adminUser.ts`（module:'admin'，GET 走 `page_size`、POST 走 form-body，复用 samisRequest/buildFormPost），含 AdminUserApi/GroupApi/MenuNodeApi 类型。

- **T3 service（token-safe）✅**：新建 `adminAggregatorService.ts`，`shouldUseReal()=useRealAdmin() && isSamisLoggedIn()`；loadAdminUsers/loadAdminUserGroups/loadMenuTree 真实走 api、失败 warning+source='mock'；create/update/delete/changePassword 真实走 api；字段映射 adapter（GH↔username、name↔name、groupid↔role、department_name↔department、is_del 反演 active）。

- **T4 store 接线 ✅**：anesthesia.ts 新增 remote 状态（remoteAdminUsers/Groups/MenuTree + source）+ actions（loadRemoteAdminUsers/Groups/MenuTree、create/update/deleteSystemUser、changeSystemPassword）+ `systemUsers` getter（真实模式且 source==='remote' → 远程，否则本地 seed）；writes 在 store 层按 `useRealAdmin()` 门控（真实调 service+刷新，mock 本地 upsert）。

- **T5/T6 两页 ✅**：SystemUsers.vue 全 CRUD（onMounted 先组后用户、角色下拉来自真组 groupid、新增/编辑 modal 含初始密码/重置密码、删除按钮+确认、数据源标签）；SystemRoles.vue 只读接真（onMounted 并发组+菜单树；真实模式展示真组 id/name/权限(catidsList)/按 groupid 统计人数 + a-tree 渲染 getMenu 菜单树；mock 回退静态 ROLE_DEFS）。SystemUser.role 由枚举改 `number(groupid)|string(mock枚举)`，buildModuleRows systemUsers desc 同步改组名展示。

- **T7 mock 路由 ✅**：samisMockRouter 增 admin 分支（adminUserList/adminUserGroupsList/getMenu 返回稳定 mock；create/update/delete/changePassword 返回成功）——关 flag 时视图门控不触发，纯防御兜底。

- **T8 验证 ✅**：
  - **vue-tsc --noEmit → 0 error**（含新建 adminUser.ts/adminAggregatorService.ts/e2e spec）。
  - **curl(token) 读路径全 `code:0`**：`adminUserList`（真实库 1 用户 quality_admin groupid 999）、`adminUserGroupsList`（1 组 999:麻醉系统管理员）、`getMenu`（9 顶层菜单节点）。
  - **curl(token) 写往返全 `code:0`**：create(E2E)→`{id:4000003}`→update(name 改)→getById 回读校验→**delete**（见下修复）→getById 返 1002(软删 is_del=1)、list 不再含该 gh；0 active E2E 残留。
  - **e2e**：新增 `e2e/real-integration-admin.spec.ts`（复用 login + `/api-samis/` 前缀守卫）——/system/users→adminUserGroupsList+adminUserList、/system/roles→adminUserGroupsList+getMenu 各 GET `code:0`+渲染+未鉴权踢回。**容器内实测 2/2 PASS（14.7s，restart samis-web 后 VITE_USE_REAL_ADMIN=true 生效）**。

- **【后端 bug 修复·delete 500】**：`AdminUser@adminUserDelete`→`UserInformationService::deleteUser:288` 误用 `$this->repository->updateUser(['id'=>$id,'is_del'=>1])` **单参**，而 repository `updateUser($where, array $data)` **需两参** → `ArgumentCountError`（TypeError）不被控制器 `catch(\Exception)` 捕获 → 500 HTML。修复：改用 repository 既有的 `deleteUser(int $id)`（`where('id',$id)->update(['is_del'=>1])`，语义一致）。1 文件 1 处，curl 复验 delete→`code:0` http200 + 软删生效。

- **【观察·非阻塞，登记】AdminUser.php:86 分页元数据 `pageSize` 仅读 camel**：`adminUserList` 控制器 `$pageSize=(int)($params['pageSize']??10)`（camel-only）用于 `api_paginate` 元数据 `page_size` 字段；前端 wrapper 传 snake `page_size` → 响应元数据 `page_size` 回显 10（非真实请求值）。**不影响功能**：实际切片由 service `getUserList:147`（已双键 T20 修复）正确读 snake 完成分页，前端 aggregator 不消费响应 page_size 元数据。与 T18/T20 同源，本轮顺手可双键（`$params['page_size']??$params['pageSize']??10`）或登记——已登记，未改。

- **role↔groupid 映射关键点**：前端弃硬编码角色枚举，SystemUser.role 真实模式存 groupid(number)、展示用组名（来自 remoteAdminUserGroups）；getMenu 使用**当前登录用户** groupid（非查询参数），故 SystemRoles 菜单树为当前管理员权限（真实库当前仅 1 组/1 用户/9 菜单节点，多角色权限树需后端按组提供或扩展 getMenu 参数）。

- **范围与卫生**：后端仅改 1 处（deleteUser）；前端新增 adminUser.ts/adminAggregatorService.ts/e2e spec + 改 apiFlags/store/2 页/mock/buildModuleRows/types/.env(.example/.local)；测试用户 create→delete 往返后 0 active 残留（含先前失败遗留的 orphan id=4000002 已清理）。既有 7 个 vitest 失败（anesthesiaSync token缺失 + qualityParity 数据）为 `.env.local` 全真模式下**环境性既有失败**，与 admin 改动无关（失败文件不在本轮 diff）。

- **回滚**：`.env.local` 删 `VITE_USE_REAL_ADMIN=true` → SystemUsers/Roles 回本地 seed/静态（mock 模式行为不变）；后端 deleteUser 修复保留（纯 bug 修复，无副作用）。

代码入口：前端 `samisWeb/src/config/apiFlags.ts`（useRealAdmin）、`samisWeb/src/api/adminUser.ts`、`samisWeb/src/services/anesthesia/adminAggregatorService.ts`、`samisWeb/src/stores/anesthesia.ts`（admin state/actions/getter）、`samisWeb/src/views/system/{SystemUsers,SystemRoles}.vue`、`samisWeb/src/services/mock/samisMockRouter.ts`（admin 分支）、`samisWeb/src/types/system.ts`（role 改 number|string）、`samisWeb/e2e/real-integration-admin.spec.ts`；后端 `index/app/samis/domain/user/service/UserInformationService.php:288`（deleteUser 改用 repository->deleteUser）。
