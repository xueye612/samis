# 麻醉记录单技术规格（合并版）

> 更新时间：2026-06-02。原分散在 `本地持久化` / `实时同步` / `冲突处理` / `设备模拟` / `后端接口` / `验收说明` 六份文档，已合并为本文件。

**仍独立维护（频繁改动或体量大）：**

- [用药显示规则](./麻醉记录单用药显示规则.md)
- [药品字典接口要求](./麻醉药品字典接口要求.md)
- [后端联调字段对照表](./麻醉记录单后端联调字段对照表.md)（完整 payload / 表映射）

---

---

# 麻醉记录单本地持久化设计

## 为什么需要本地持久化

麻醉记录单是基于时间轴的连续记录，包含高频生命体征、用药线段、液体/输血、事件符号等。当前 samisweb 原型在刷新后会丢失运行态数据，无法满足术中连续记录与断网场景。

## 为什么使用 IndexedDB

- 结构化、异步、容量大，适合高频写入与批量查询
- 支持事务，适合「写记录 + 写队列 + 写审计」的原子组合
- localStorage 仅保存轻量偏好（当前病例、页码等），不存完整记录单

## IndexedDB 与 localStorage 分工

| 存储 | 内容 |
|---|---|
| IndexedDB `samis_anesthesia_local_v1` | 完整病例、子表、同步队列、设备 raw |
| localStorage `samis.anesthesiaRecord.localState.v1` | 字典覆盖、UI draft（原有逻辑保留） |
| IndexedDB `settings` | 当前页码、最近打开记录 |

## 本地表结构

数据库：`samis_anesthesia_local_v1`（Dexie，见 `src/services/anesthesia/localDb.ts`）

| 表 | 说明 |
|---|---|
| records | 主记录 + `case_payload` 全量 JSON |
| snapshots | 打印/锁定快照 |
| timeline_events | 事件 |
| medications / fluids / transfusions | 用药、液体、输血 |
| vital_signs | 趋势图/打印显示点 |
| monitor_raw / ventilator_raw | 设备原始数据 |
| io_records / lab_results | 出入量、血气 |
| audit_logs | 本地审计 |
| sync_queue | 待上传队列（仅前端） |
| settings | 页码等偏好 |

## local_id 与 server_id

- 前端生成 `local_id`（如 `case-001`、`vital-173...`）
- 后端返回 `server_id` 后写入对应行与 `records.server_id`
- 幂等键：`operation_id + entity_type + local_id`

## Hydration 状态

store 暴露三个标志（见 `src/stores/anesthesia.ts`）：

| 标志 | 含义 |
|---|---|
| `localPersistenceReady` | IndexedDB 初始化完成 |
| `isHydrating` | 正在从 IndexedDB 读取并合并数据 |
| `hasRestoredLocalData` | 至少合并过一条本地记录 |

流程：

```text
页面进入 / app 启动
  → isHydrating = true
  → 读取 records / settings / sync_queue / conflicts
  → 合并本地 case 覆盖 seed（仅本地有记录时）
  → restoreCasePageNo（页码在记录单渲染前完成）
  → isHydrating = false, localPersistenceReady = true
  → 渲染 LiveAnesthesiaSheet
```

`main.ts` 在 `app.mount()` 前 `await bootstrapAnesthesiaLocalPersistence()`，避免先渲染 seed 再闪变本地数据。

## 刷新恢复逻辑

1. 应用启动：`bootstrapAnesthesiaLocalPersistence()` 从 IndexedDB 合并；**无本地记录时才保留 seed mock**
2. hydration 完成前：`AnesthesiaRecord.vue` 显示 `--record-sheet-skeleton-height` 骨架，不渲染完整记录单主体
3. 打开记录单：`restoreCasePageNo(caseId)` 在 `sheetReady` 为 true 前完成
4. 优先使用本地 `case_payload` 覆盖 seed 中同 id 病例
5. 子表用于同步与设备追溯；UI 主渲染仍来自 `SurgeryCase`

## 页面刷新防抖动

- 不在 hydration 前渲染 `LiveAnesthesiaSheet` 与趋势图
- 顶部状态栏固定高度 32px，chip 出现/消失不改变页面高度
- 设备状态恢复不触发 `syncRecordDocument` 全量重绘
- 切换病例时短暂 `caseSheetReady = false`，避免旧数据闪屏
- 骨架高度使用 CSS 变量 `--record-sheet-skeleton-height`，随 `sheetZoom` 与多页数微调，减少 hydration 完成时高度跳变

## 顶部状态栏

- 组件：`RecordStatusBar.vue`，单行固定 32px
- **仅全局摘要**：待上传（可点击→同步详情弹窗）、冲突（可点击→冲突面板）、失败、同步中、网络在线/离线、最近同步时间
- 本地已保存时间移入「同步详情」弹窗，不在顶部重复
- 成功态弱化（`muted` chip）；失败/冲突突出（红/橙 chip）
- chip 固定高度 22px，出现/消失不改变状态栏 32px 高度
- 打印/打印预览：`@media print` + `.no-print` 隐藏

## 右侧设备与同步面板

- 组件：`RecordDeviceWorkbenchPanel.vue`（折叠项「设备与同步」）
- **仅设备运行摘要 + 详情入口**：监护仪/呼吸机运行状态（行内启动/停止）、最近采集、采样频率只读 chip
- 同步详情 / 冲突入口为文本按钮；失败时显示「同步失败，点击查看」
- 模拟模式、异常类型、可编辑采样间隔折叠在「模拟设置」`<details>` 内
- 核心区域高度约 160px 内，不使用 2×3 大卡片
- 不与顶部重复展示待上传/网络/采集状态/模拟模式摘要
- 打印/打印预览：随 `.record-toolbox` 隐藏

## 开发测试按钮

- 「注入测试冲突」仅在 `import.meta.env.DEV && VITE_ANESTHESIA_USE_MOCK` 时显示
- 生产/演示构建默认隐藏，不出现在临床操作界面

## 全局 bootstrap 门控

- `main.ts`：`app.mount()` 前 `await bootstrapAnesthesiaLocalPersistence()`
- `router.beforeEach`：导航前再次确保 bootstrap 完成（幂等）
- `AppLayout`：`localPersistenceReady && !isHydrating` 前显示轻量 loading，避免业务页误读 seed
- composable：`useAnesthesiaPersistenceGate()` / `ensureAnesthesiaPersistenceReady()`
- **原则**：IndexedDB 有数据 → 合并覆盖 seed；无本地数据 → seed 作为演示 fallback，不作为 hydration 前“真实数据”展示

## 自动保存

- 入口：`store.afterRecordMutation(caseId)` → `schedulePersistCase()` 防抖 1s
- 每次保存：写 records + 子表 + sync_queue（除非 `skipQueue`）
- 页码变更：`livePageNo` watch → `setRecordPageDraft` + 持久化

## 手动保存

「保存草稿」仍写 localStorage UI draft；临床数据通过 IndexedDB 自动保存。

## 锁定/打印

`printAndLockRecord` 触发 `operationType: lock` 入队，快照写入 `snapshots`。

## 清理策略

实现：`src/services/anesthesia/anesthesiaLocalCleanupService.ts`

| 函数 | 作用 |
|---|---|
| `cleanupSyncedDeviceRaw()` | 清理非当前记录、已同步、超过 7 天的 raw |
| `cleanupOldSuccessQueue()` | 清理超过 3 天的 success 队列 |
| `getLocalStorageStats()` | 统计 raw / queue 数量 |
| `runConservativeLocalCleanup()` | 手动触发保守清理 |
| `runStartupLocalCleanupIfDue()` | 启动后每 6 小时最多执行一次 |

保守规则：

- **不清理**：`sync_status !== success` 的 raw、`pending/failed/conflict/uploading` 队列、当前打开记录全部 raw
- **可清理**：非当前记录 + 已同步 + 超过 7 天的 `monitor_raw` / `ventilator_raw`；超过 3 天且不在当前记录最近 200 条内的 success 队列
- 清理结果写入 `audit_logs`（action=`local_cleanup`）

## 性能注意

- 防抖 1s，避免每次按键写库
- 设备 raw 与 vital_signs 分离；趋势图只读 display 点
- 批量上传设备数据，不每条立即请求后端

## 跨页 vital_signs 显示规则

实现：`recordPaginationEngine.isTimeOnPage` + `LiveAnesthesiaSheet.pageVitals` + `selectDisplayVitalsForBand`

| 规则 | 说明 |
|---|---|
| 页内归属 | 非末页：`pageStartTime <= measure_time < pageEndTime` |
| 末页 | `pageStartTime <= measure_time <= pageEndTime`（包含轴终点） |
| 下一页 | `nextStartTime <= measure_time < nextEndTime` |
| 分桶 | 趋势图按 `vitalDisplayIntervalMinutes` 分桶，每桶保留优先级最高的一条 |
| 禁止 | 页外数据不得挤压到页尾；边界点不得在相邻两页重复显示 |

`selectDisplayVitalsForBand` 仅对**当前页已过滤**的 vitals 分桶，抢救模式 1 分钟/条时桶宽为 1，避免同 x 坐标堆叠。

## 空字段提示策略

| 模式 | 纸面行为 |
|---|---|
| 编辑 | picker 类字段 hover 显示选择入口；`PaperFormField` 空值浅色占位，无大面积虚线 |
| 查看 | 只显示文本或空白，不显示选择按钮 |
| 打印 | 隐藏虚线、选择按钮、hover、调试提示 |
| 待完善 | 手术体位、麻醉方法、医师/护士、实施手术、术后去向、麻醉小结等进入右侧「待完善列表」，不在纸面强提示 |

实现：`PaperPickerField` / `AnesthesiaMethodPickerField` / `PaperFormField` 的 `interactionMode`；`buildRecordPendingFields()` → `RecordQualityPanel`。

## 用药子表字段（medications）

`medications` 行除 `payload` 全量 JSON 外，索引字段包括：

| 字段 | 说明 |
|---|---|
| `mode` | `single` / `continuous` / `intermittent`（前端录入为 单次用药/持续泵入/间断追加） |
| `is_special` | 是否进入纸面「辅助及特殊用药」说明区；**不**决定是否画线 |
| `special_no` | 特殊用药区编号（1、2、3…，展示转 ①②③） |
| `event_time` | 单次/间断给药事件时间 |
| `start_time` / `end_time` | 持续泵注区间 |
| `rate` | 泵速等（对应前端 `pumpRate`） |

显示规则详见 [麻醉记录单用药显示规则.md](./麻醉记录单用药显示规则.md)。


---

# 麻醉记录单实时同步设计

## 同步队列

表：`sync_queue`（IndexedDB）

字段：`queue_id, batch_no, entity_type, operation_type, api_path, payload, status, retry_count, next_retry_at`

状态：

| status | 含义 |
|---|---|
| pending | 待上传 |
| uploading | 批次上传中 |
| success | 已成功 |
| failed | 失败，等待重试 |
| conflict | 服务端冲突（mock 暂未模拟） |

## local_id / server_id 映射

上传成功后 `anesthesiaSyncService` 将 `server_id` 写回对应实体表与 queue 行。

## batch_no 规则

```text
front_YYYYMMDD_HHmmss_###
monitor_YYYYMMDD_HHmmss_###
ventilator_YYYYMMDD_HHmmss_###
```

## 实时上传策略

| 数据类型 | 策略 |
|---|---|
| 手工事件/用药/液体 | 入队后 ~1.5s 批量上传 |
| vital_sign 显示点 | ~8s 批量 |
| monitor_raw / ventilator_raw | ~15s 批量 |
| 扫描兜底 | 每 5s 扫描 pending |

## 断网补传

- `navigator.onLine` 监听 offline/online
- 离线：继续本地保存 + 队列 pending
- 恢复在线：立即 `flushAnesthesiaSyncNow()`

## 失败重试

指数退避：`5 * 2^retry_count` 秒，上限 300s，写入 `next_retry_at`

## 高频设备节流

设备 mock 每 3~5s 写 raw，但上传合并为 15s 批次，避免打满网络。

## 冲突处理

版本冲突、锁定冲突、人工修正生命体征冲突等**不会自动覆盖**，详见本文「同步冲突处理」章节。

- 冲突写入 `sync_conflicts` 表
- 队列项 status=`conflict` 停止自动重试
- 页面顶部显示冲突数量，打开冲突面板处理

## UI 状态区收口（2026-06-02）

| 区域 | 职责 | 交互 |
|---|---|---|
| 顶部 `RecordStatusBar` | 全局同步摘要 | 点击「待上传 N」→ 同步详情弹窗；点击「冲突 N」→ `SyncConflictPanel` |
| 右侧「设备与同步」 | 设备运行摘要 + 详情入口 | 「同步详情」「冲突 N」按钮；失败弱链「同步失败，点击查看」 |
| 质控侧栏 | 异常/完整度/缺陷 | **不再**重复「同步详情」折叠块 |

- **同步成功**：不弹窗；仅更新最近同步时间、顶部 chip、待上传数量
- **同步失败**：顶部红色失败 chip + 右侧弱提示链接
- **冲突**：顶部 + 右侧按钮入口；不自动弹大面板（签名/提交前校验除外）
- **打印/预览**：隐藏状态栏、设备面板、同步/冲突入口、开发测试按钮

## success 队列清理

实现：`cleanupOldSuccessQueue()`（`anesthesiaLocalCleanupService.ts`）

| 规则 | 说明 |
|---|---|
| 不清理 | `pending` / `uploading` / `failed` / `conflict` |
| 保留 | 当前打开记录最近 **200** 条 success |
| 可清理 | 其他 success 且 `updated_at` 超过 **3 天** |
| 映射 | `local_id` / `server_id` 保存在实体表，清理 queue 不影响映射 |

触发方式：启动后低频自动（6h）或 `runConservativeLocalCleanup()` 手动。

## 幂等要求

每条 payload 含 `localId, syncVersion, operationId, recordLocalId`

## 后端接口

主批次：

```text
POST /api-samis/pc/v1/anesthesiaSync/pushBatch
```

## mock 与真实切换

- 默认 mock：`backendFetch` 路由（`src/services/mockApi.ts`）
- 关闭 mock：设置 `VITE_ANESTHESIA_USE_MOCK=false` 并接入真实 request（后续）
- 页面层只调用 `src/api/anesthesiaSync.ts` 等 service

## 历史接口说明

`/api/anesthesia/...` 与 `/api/anes/...` 为历史参考，**不是**当前主接口风格。


---

# 麻醉记录单同步冲突处理设计

## 1. 设计目标

本地 IndexedDB 与服务器 OceanBase 同步时，**不能**简单本地覆盖服务器，也**不能**简单服务器覆盖本地。发生冲突时必须显式记录、提示用户并支持人工决策。

## 2. 同步元数据要求

### 2.1 本地实体行（所有业务表）

每条本地记录必须包含：

| 字段 | 说明 |
|---|---|
| local_id | 前端生成 UUID |
| server_id | 后端返回，可为空 |
| sync_version | 单调递增版本 |
| sync_status | local_only / pending / success / failed / conflict |
| updated_at | 最后更新时间 |
| deleted_at | 软删除时间（void 时不物理删） |
| void_reason | 作废原因 |

### 2.2 上传队列 sync_queue

| 字段 | 说明 |
|---|---|
| operation_type | create / update / delete / void / lock / print |
| entity_type | record / vital_sign / medication / ... |
| local_id | 实体 local_id |
| server_id | 已知 server_id |
| base_sync_version | 上传时所依据的本地版本 |
| payload | 完整上传体 |

## 3. 冲突表 sync_conflicts

| 字段 | 说明 |
|---|---|
| conflict_id | 主键 |
| local_payload | 本地快照 JSON |
| server_payload | 服务器快照 JSON |
| conflict_type | version_mismatch / record_locked / vital_corrected / ... |
| resolve_status | pending / resolved / ignored |
| resolve_action | use_server / keep_local_correction / manual_merge / ignore_local / retry_sync |

## 4. 同步流程

```text
本地变更 → 写 IndexedDB + 入队(baseSyncVersion)
  ↓
pushBatch 逐条校验
  ↓
success → 写回 server_id + sync_version
failed  → 指数退避重试
conflict → 写 sync_conflicts + 队列 status=conflict
  ↓
用户打开冲突面板 → 选择处理策略 → 重新保存/重试
```

## 5. 冲突判断规则

| 场景 | 策略 |
|---|---|
| baseSyncVersion < serverSyncVersion（用药/液体/输血/事件） | **conflict**，不自动覆盖 |
| 服务器 record locked/printed + 本地 update/void | **conflict** |
| vital is_corrected=true + 设备数据写入 | **conflict** / 本地拒绝覆盖 |
| monitor_raw / ventilator_raw 重复 local_id 或 collect_time | **dedup success**，追加为主 |
| delete 操作 | 传 void + deleted_at + void_reason，服务器软作废 |

## 6. 幂等要求（后端）

同一 `record_id + entity_type + local_id` 重复 pushBatch：

- 已存在 → 返回 success + 原 server_id（不重复插入）
- 版本冲突 → 返回 conflict + serverPayload + serverSyncVersion

## 7. pushBatch 返回要求

每条 item 必须返回：

```json
{
  "entityType": "medication",
  "localId": "med-001",
  "serverId": 10002,
  "status": "success|failed|conflict",
  "serverSyncVersion": 3,
  "conflictType": "version_mismatch",
  "serverPayload": {},
  "message": ""
}
```

## 8. 冲突处理动作

| 动作 | 行为 |
|---|---|
| 使用服务器版本 | 本地 case 应用 server_payload |
| 保留本地修正 | 以手工修正重新入队，sync_version+1 |
| 人工合并 | 合并 JSON 后保存并重试 |
| 忽略本地 | 丢弃本地 pending，采用服务器 |
| 重试同步 | 队列重新 pending，携带最新 baseSyncVersion |

## 9. 页面提示

记录单顶部显示「冲突 N」，点击进入 `SyncConflictPanel` 查看 local/server 对比并处理。

## 10. 验收方式

1. mock 服务器先写入 medication syncVersion=2
2. 本地 baseSyncVersion=1 上传 → 收到 conflict
3. sync_conflicts 有记录，顶部冲突数 +1
4. 选择「使用服务器版本」→ 冲突清零，本地数据与 server_payload 一致
5. 锁定记录后本地修改 → conflict_type=record_locked
6. 人工修正 vital 后启动监护仪模拟 → 不产生覆盖（conflict 或跳过写入）

## 11. 代码入口

- `src/services/anesthesia/anesthesiaSyncConflict.ts`
- `src/services/anesthesia/anesthesiaSyncService.ts`
- `src/components/anesthesia/record/SyncConflictPanel.vue`
- mock：`src/services/mockApi.ts` → `handleSamisSyncPushBatch`


---

# 麻醉设备模拟采集设计

## 设备模拟模式（2026-06-02）

右侧「设备与同步」工作台提供三种模式（`deviceSimulationMode.ts` + `RecordDeviceWorkbenchPanel`）：

| 模式 | raw 频率 | vital_signs 显示频率 | 异常 | 采集状态 |
|---|---|---|---|---|
| 正常模拟 | 4s/条 | 5 分钟/条（可配置 1–5） | 极少，不刷屏 | 采集中 |
| 异常模拟 | 4s/条 | 5 分钟/条 | 可控异常，可选类型 | 采集中 |
| 抢救模式 | 2s/条 | 1 分钟/条 | 可偶发混乱 | 抢救采集中 |

localStorage：

- `samis.anesthesia.deviceSimulationMode`
- `samis.anesthesia.abnormalSimulationTypes`

### 正常模拟

日常演示，稳定范围（`deviceMockSamples.ts`）：

```text
HR: 65–95
SBP: 100–135
DBP: 60–85
SpO2: 96–100
TEMP: 36.0–37.2
EtCO2: 35–45
```

### 异常模拟

- 不进入抢救高频模式
- 每个 vital 显示桶写入一条**选定类型**的异常值
- raw 约 25% 概率带异常，用于追溯
- 可选类型：低血压、高血压、心率过快、血氧下降、EtCO2 异常
- 右侧面板仍遵循「连续/严重才提醒」（`aggregateAbnormalVitalsForPanel`）

### 抢救模式

- 自动进入记录单抢救状态（`enterRescueRecordMode`）
- raw 2s、vital 1 分钟/条
- 趋势图按 1 分钟桶 + 页过滤，避免点位堆叠
- 退出抢救模式：顶部「退出抢救」或切回正常/异常模拟

## 监护仪 mock

文件：`src/services/anesthesia/monitorMockService.ts`

字段：HR, SBP, DBP, MAP, SpO2, TEMP, RR, BIS

正常范围：

| 项 | 范围 |
|---|---|
| HR | 60-100 |
| SBP | 90-140 |
| DBP | 50-90 |
| SpO2 | 95-100 |
| TEMP | 35.5-37.5 |
| RR | 10-20 |
| BIS | 40-60 |

频率：默认 **5 分钟** 写入 1 条 `monitor_raw` + 1 条 `vital_signs`；可在记录单页「监护仪」区配置为 **1–5 分钟/条**（localStorage 持久化）。抢救模式下固定 1 分钟/条。

## 呼吸机 mock

文件：`src/services/anesthesia/ventilatorMockService.ts`

字段：vent_mode, tidal_volume, respiratory_rate, fio2, peep, peak/plateau pressure, minute_volume, etco2, airway_pressure

频率：同监护仪；ETCO2/RR/气道压写入 vital 显示点

## 异常模拟（历史）

旧版约 8% 随机异常已废弃，改为上述三种模式独立控制。

## 抢救模式（历史说明）

`rescueMode: true` 时间隔 **1 分钟/条**（覆盖用户配置的 1–5 分钟）

## raw 本地保留策略

见 `anesthesiaLocalCleanupService.ts`：

- 未同步（`local_only` 等）raw **禁止**清理
- 当前打开记录 raw **全部保留**
- 非当前记录、已同步、超过 7 天 raw 可低频清理
- 趋势图只读 `vital_signs`，清理 raw 不影响纸面显示（已同步部分）

## raw 与 display 点

| 表 | 用途 |
|---|---|
| monitor_raw / ventilator_raw | 追溯、批量上传 |
| vital_signs | 趋势图、打印 |

打印只使用 `vital_signs`，不直接使用 raw。

## 上传队列

raw → `entity_type: monitor_raw|ventilator_raw`

vital → `entity_type: vital_sign`

API：

```text
POST /api-samis/pc/v1/anesthesiaDevice/batchPushMonitorData
POST /api-samis/pc/v1/anesthesiaDevice/batchPushVentilatorData
```

## 数据边界（禁止写入患者/手术快照）

设备模拟**只**负责设备相关数据，**不得**写入或修改：

- 患者姓名、性别、年龄、住院号、床号、科室、诊断
- 手术名称、手术通知、麻醉方法、手术人员
- `snapshots`、`operationInfo`、`record snapshot`

允许写入：

| 目标 | 说明 |
|---|---|
| `monitor_raw` / `ventilator_raw` | 设备原始采集 |
| `vital_signs` | 趋势图/打印显示点 |
| `sync_queue` | 待上传队列 |
| `records.case_payload.device` | 最近采集时间、运行状态等元数据 |

患者/手术信息**仅**来源于 `operationInfo/getOperationInfo` 或本地已保存的 `snapshots`。

实现要点：

- `monitorMockService` / `ventilatorMockService` 不调用 `saveCaseToLocalDb` 全量保存
- store 使用 `saveDeviceVitalOnly` / `patchRecordDeviceCollectMeta`，不触发 `syncRecordDocument`
- 设备 tick 只追加 `vitals` 数组，不覆盖完整 `SurgeryCase`

## 启动前校验

启动监护仪/呼吸机模拟前必须满足：

1. 本地持久化已完成（`localPersistenceReady`）
2. 当前已打开麻醉记录（`record_local_id` / `selectedId`）
3. 记录含 `operation_id` 与 `patient_id`
4. 记录未锁定

不满足时提示：**请先打开或创建麻醉记录单后再启动设备模拟**

## 刷新与状态恢复

- 刷新后**不自动启动**监护仪/呼吸机
- 若模拟未运行，状态栏不显示「运行中」
- 可显示最近一次采集时间
- 恢复设备元数据时**不修改**患者信息

## 状态提示位置

- **顶部** `RecordStatusBar`：仅全局同步摘要（待上传、冲突、网络、最近同步），**不**展示设备运行详情
- **右侧**「设备与同步」：监护仪/呼吸机运行状态、最近采集、采样频率 chip；同步/冲突为详情入口
- 采样间隔在主面板为只读 chip（`采样 N 分钟/条`）；可编辑时仅在「模拟设置」内
- 设备启动/停止/采集**不弹大提示**；同步成功仅更新时间 chip
- 打印模式下隐藏状态栏、设备面板、开发测试按钮及所有运行态 UI

## 趋势图刷新

mock 写入 `vital_signs` 后通过 `onDeviceVitalAppended` 轻量更新内存中的 `vitals` 数组，**不**调用 `syncRecordDocument()`。

### 横轴与监测带定位

- 趋势图横轴使用 **含秒的小数分钟**（`timeToFractionalMinutes`），避免设备 3–5s 采集全部落在同一分钟列造成符号堆叠。
- 监测数值带对齐 **5 分钟纸面格**，同一格同一指标只保留 **最新一条** 显示值（`buildMonitorCells` 网格去重）。
- 设备采集时间锚定手术日期（`resolveRecordSheetNowIso`），与记录单时间轴一致。


---

# 麻醉记录单后端接口要求

> **联调主文档（字段对照 / payload / 冲突契约）**：见 [麻醉记录单后端联调字段对照表.md](./麻醉记录单后端联调字段对照表.md)

## 风格约定

参考现有护理/samis 接口：

```text
GET /api-samis/pc/v1/operationInfo/getOperationInfo
```

麻醉模块统一前缀：

```text
/api-samis/pc/v1/anesthesiaRecord/*
/api-samis/pc/v1/anesthesiaSync/*
/api-samis/pc/v1/anesthesiaDevice/*
/api-samis/pc/v1/anesthesiaDict/*
```

**不再**把 `/api/anesthesia/records` 或 `/api/anes/*` 作为 samisweb 当前主接口。

## mock 与真实切换

| 项 | 说明 |
|---|---|
| 当前 | `backendFetch` mock（`src/services/mockApi.ts`） |
| 环境变量 | `VITE_ANESTHESIA_USE_MOCK`（默认 true） |
| 切换 | 设为 `false` 且后端可用时，同一 service 改走真实 HTTP，页面无感 |
| 前端禁止 | 直连 OceanBase；链路为 前端 → IndexedDB/队列 → API → DB |

## 初始化：手术通知单详情

```text
GET /api-samis/pc/v1/operationInfo/getOperationInfo?operationId=...
```

用途：

- 初始化患者信息、手术信息
- 写入 IndexedDB `snapshots`
- 获取 `operation_id` / `patient_id` / `room_id`
- 打印不依赖实时 operationInfo

## 返回结构

沿用项目统一封装：

```json
{
  "code": 0,
  "msg": "success",
  "data": {},
  "request_id": "mock-173..."
}
```

## 前端 service 层

| 文件 | 职责 |
|---|---|
| src/api/anesthesiaRecord.ts | 记录 CRUD |
| src/api/anesthesiaSync.ts | 批量同步 |
| src/api/anesthesiaDevice.ts | 设备批量上传 |
| src/api/anesthesiaDict.ts | 字典 |

## 真实后端联调清单

### 1. 麻醉记录单

| 接口 | 用途 | 前端状态 |
|---|---|---|
| getRecordDetail | 打开记录单拉取详情 | mock |
| createRecord | 首次创建 | mock |
| saveRecord | 保存主记录 | mock |
| saveSnapshot | 锁定/打印快照 | mock |
| batchSaveTimelineEvents | 批量事件 | mock |
| saveMedication | 单条用药保存 | mock 待对齐 |
| batchSaveMedications | 批量用药 | mock |
| batchSaveFluids | 批量液体 | mock |
| batchSaveTransfusions | 批量输血 | mock |
| batchSaveVitalSigns | 批量生命体征 | mock |
| saveIoRecord | 出入量 | mock |
| saveLabResult | 血气 | mock |
| lockRecord | 锁定 | mock |
| voidRecord | 作废 | mock |

#### 用药保存字段（saveMedication / batchSaveMedications）

```http
POST /api-samis/pc/v1/anesthesiaRecord/saveMedication
POST /api-samis/pc/v1/anesthesiaRecord/batchSaveMedications
```

请求体（camelCase 示例）须支持：

| 字段 | 说明 |
|---|---|
| `mode` | `single` / `intermittent` / `continuous` |
| `isSpecial` | **医生最终确认**；不得仅凭 drugName 强制覆盖 |
| `specialNo` | 特殊用药序号（可前端生成） |
| `specialCategory` | 特殊用药分类 |
| `specialReason` | 辅助及特殊用药区说明 |
| `eventTime` | 单次/间断事件时间 |
| `startTime` / `endTime` | 持续区间 |
| `dose` / `doseUnit` / `rate` / `rateUnit` / `route` | 剂量与途径 |

后端缺省规则：仅当请求**未传** `isSpecial` 时，可按药品字典 `defaultIsSpecial` 补齐；若前端显式传入 `isSpecial`，必须原样保存。

药品字典接口见 [麻醉药品字典接口要求.md](./麻醉药品字典接口要求.md)。显示规则见 [麻醉记录单用药显示规则.md](./麻醉记录单用药显示规则.md)。

前端「用药记录」弹窗仅调整录入 UI，保存字段与上述接口一致，不改变 `mode` / `isSpecial` 语义。

### 2. 字典

| 接口 | 用途 |
|---|---|
| getDrugDict | 麻醉药品字典（含 defaultIsSpecial 等） |
| saveDrugDict / disableDrugDict | 字典维护 |
| getSpecialDrugCategories | 特殊用药分类 |
| getDrugRecommend | 用药推荐（可选） |

详见 [麻醉药品字典接口要求.md](./麻醉药品字典接口要求.md)。

### 3. 同步

| 接口 | 用途 |
|---|---|
| pushBatch | 批量上传队列 |
| getSyncStatus | 同步状态 |
| resolveConflict | 冲突处理 |

队列字段（`sync_queue`）：

| 字段 | 说明 |
|---|---|
| local_id | 前端实体本地 ID |
| server_id | 后端返回后回填 |
| baseSyncVersion | 上传前实体版本 |
| syncVersion | 当前版本 |
| entityType | vital_sign / medication / monitor_raw 等 |
| operationType | create / update / delete / lock |
| payload | 实体 JSON |

pushBatch 返回（mock 已实现，真实需对齐）：

```json
{
  "success": [{ "localId": "...", "serverId": 1, "syncVersion": 2 }],
  "failed": [{ "localId": "...", "errorCode": "...", "message": "..." }],
  "conflicts": [{
    "localId": "...",
    "conflictType": "version_mismatch",
    "localPayload": {},
    "serverPayload": {},
    "localSyncVersion": 2,
    "serverSyncVersion": 3
  }]
}
```

### 3. 设备

| 接口 | 用途 |
|---|---|
| batchPushMonitorData | 监护仪 raw 批量 |
| batchPushVentilatorData | 呼吸机 raw 批量 |
| getLatestDeviceData | 拉取最新设备数据 |

**当前阶段结论（联调约定）**：

```text
前端上传 raw + vital_signs；
后端先原样保存；
后续真实设备接入后，再决定是否由后端统一抽样生成 vital_signs。
```

| 问题 | 当前结论 |
|---|---|
| 后端是否生成 vital_signs | 暂不强制，可先原样存前端 vital |
| 前端是否只上传 raw | 否，当前 raw + vital 均入队 |
| 幂等 | operation_id + entity_type + local_id |
| 重复上传 | 同 local_id 覆盖或返回已有 server_id |

## 等待 TP6 后端实现的接口

上述 `/api-samis/pc/v1/anesthesiaRecord/*`、`anesthesiaSync/*`、`anesthesiaDevice/*` 全部为 **mock 完成、真实待开发**。

关闭 mock 前需确认：

1. 鉴权与护理系统统一
2. pushBatch 冲突结构对齐
3. 设备 batch 接口支持幂等
4. getOperationInfo 与 IndexedDB snapshots 字段映射

## 历史参考（非主接口）

- `/anesthesia/records` legacy mock 路由仍保留
- shuhu-surgianes 仓库中 `/api/anes/*` 为护理后端规划（非 samisweb 主链路）


---

# 麻醉记录单持久化与设备模拟验收说明

## 1. 刷新不丢数据

1. 打开麻醉记录单，添加事件、用药、液体、生命体征
2. 切换页码到第 2 页（如有多页）
3. F5 刷新
4. 预期：趋势图、用药线、事件、页码均恢复

## 2. 检查 IndexedDB

1. Chrome DevTools → Application → IndexedDB → `samis_anesthesia_local_v1`
2. 查看 `records` 表 `case_payload`
3. 查看 `medications` / `vital_signs` / `timeline_events` 子表

## 3. 本地队列

1. 修改记录后顶部显示「待上传 N」
2. Application → `sync_queue` 出现 pending 行
3. 刷新后 pending 仍在

## 4. mock 同步

1. 保持在线，等待 ~5–15s
2. 顶部出现「同步成功 HH:mm:ss」
3. sync_queue 对应行 status=success
4. Network 无真实请求（mock 内存路由）

## 5. 失败重试

1. DevTools → Network → Offline
2. 继续编辑，pending 增加
3. 恢复 Online，pending 下降

## 6. 监护仪模拟

1. 点击「启动监护仪模拟」
2. 3–5s 内趋势图新增点；`monitor_raw` 有数据
3. 「停止监护仪模拟」后不再新增

## 7. 呼吸机模拟

1. 点击「启动呼吸机模拟」
2. ETCO2/RR 相关曲线更新；`ventilator_raw` 有数据

## 8. 打印结构

1. 打印预览布局与改动前一致
2. 锁定/打印流程仍可用

## 9. 刷新不抖动

1. 打开已有本地数据的麻醉记录单
2. F5 刷新
3. 预期：先见固定高度 loading 骨架，完成后一次呈现记录单；无明显「先 mock 再本地」闪变
4. 页码与趋势图稳定，不重复闪烁初始化

## 10. 状态提示不遮挡记录单

1. 观察顶部 32px 状态栏（`RecordStatusBar`）：待上传、冲突、网络、最近同步
2. 修改记录、产生 pending/冲突、启动设备时，记录单打印区域宽高不变
3. 右侧「设备与同步」默认紧凑（约 160px 核心区），不挤压下方方案/麻醉方式面板
4. 进入打印预览或 `@media print`：状态栏、设备面板、同步/冲突入口隐藏

## 11. 设备模拟不修改患者信息

1. 记录患者姓名、性别、年龄、诊断、手术名称
2. 启动监护仪模拟，等待若干采集周期
3. 预期：上述字段不变；`monitor_raw` / `vital_signs` 有新增；趋势图更新
4. 启动/停止呼吸机同理，仅 ETCO2/RR/气道压等相关点变化
5. IndexedDB `snapshots` 与 `case_payload` 中 patient/operation 字段未被设备 tick 改写

## 12. 未打开记录单禁止设备模拟

1. 未选择病例或记录缺少 operation_id/patient_id 时点击「启动监护仪模拟」
2. 预期：提示「请先打开或创建麻醉记录单后再启动设备模拟」，设备不启动

## 13. 本地清理策略

1. 单元测试：`anesthesiaLocalCleanupService.test.ts`
2. 预期：`pending` / `failed` 队列仍在；未同步 raw 仍在
3. 非当前记录、已同步、超过 7 天 raw 可被清理
4. 超过 3 天的 success 队列可清理（当前记录最近 200 条 success 保留）

## 14. Playwright E2E

```bash
npm run test:e2e -- e2e/anesthesia-persistence.spec.ts
npm run test:e2e -- e2e/anesthesia-device-mock.spec.ts
npm run test:e2e -- e2e/anesthesia-print-layout.spec.ts
```

覆盖：刷新恢复、设备不改患者信息、状态栏不挤压打印区、打印媒体隐藏状态栏。

## 16. 设备模拟三模式

1. 打开右侧「设备与同步」
2. 切换：正常模拟 / 异常模拟 / 抢救模式
3. 正常：vitals 稳定，5 分钟/条，raw 4s
4. 异常：勾选异常类型，启动监护仪，右侧面板在连续异常后出现提醒
5. 抢救：显示「抢救采集中」，1 分钟/条，raw 2s，趋势图无同 x 堆叠

## 17. 跨页 vital 验收

1. 长时段病例（或多页）
2. 页边界附近 vital：当前页 `[start, end)`，末页含 end
3. 切换页码：边界点只出现在一页
4. 单元测试：`recordPaginationEngine.test.ts`

## 18. 冲突 E2E 验收

1. **开发模式 + mock**（`import.meta.env.DEV && VITE_ANESTHESIA_USE_MOCK`）下可见「注入测试冲突」
2. 点击「注入测试冲突」（或使用 `window.__samisAnesthesiaE2E.injectConflict`）
3. 顶部「冲突 N」→ 打开面板 → 查看 local/server payload
4. 「使用服务器版本」或「重试同步」后冲突数减少

## 19. 打印态验收

1. 更多 → 打印预览
2. 不显示：状态栏、设备面板、注入测试按钮、picker 选择按钮、蓝色虚线
3. 记录单主体完整

## 20. 待完善列表

1. 清空手术体位/麻醉方法等字段
2. 右侧「待完善列表」出现对应项
3. 纸面无大面积强提示

## 21. 用药划线与特殊用药显示（2026-06-02）

| # | 场景 | 操作 | 预期 |
|---|---|---|---|
| M1 | 普通持续 | 瑞芬太尼 09:00–10:30，持续泵入，非特殊 | 用药带线段；特殊用药区无 |
| M2 | 特殊持续 | 去甲肾上腺素 09:30–10:40，持续 + 特殊用药 | 用药带线段 + 下方编号说明 |
| M3 | 普通单次 | 罗库溴铵 09:05 50mg 单次 | 时间点剂量；不画线 |
| M4 | 特殊单次 | 麻黄碱 10:15 6mg 单次 + 特殊用药 | 时间点圈号；特殊区有说明 |
| M5 | 间断追加 | 舒芬太尼多条「间断追加」 | 多个时间点；无连续线 |
| M6 | 液体/输血 | 复方氯化钠/红细胞带结束时间 | 输液/输血 band 线段 |

自动化：`npm run test -- --run src/services/medicationDisplayRules.test.ts src/services/drugDictRecommend.test.ts`（17 条通过即规则未回归）。

## 22. 特殊用药推荐与医生覆盖

| # | 场景 | 操作 | 预期 |
|---|---|---|---|
| S1 | 默认特殊药 | 选「去甲肾上腺素」新建 | 自动勾选特殊用药；说明「维持血压」；可取消 |
| S2 | 默认普通药 | 选「舒芬太尼」 | 默认不勾选；可手动勾选 |
| S3 | 间断非特殊 | 舒芬太尼多条「追加/间断」、不勾选特殊 | 多时间点；无圈号；不进说明区 |
| S4 | 取消推荐 | 选麻黄碱后取消特殊勾选并保存 | `is_special=false`；无说明区条目 |
| S5 | 手动标特殊 | 舒芬太尼单次 + 勾选特殊 | 有圈号与说明区；不画线 |

