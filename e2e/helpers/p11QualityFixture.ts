import { execFile } from 'node:child_process';
import { promisify } from 'node:util';
const execFileAsync=promisify(execFile);const container=process.env.SAMIS_E2E_FIXTURE_CONTAINER??'1Panel-php8-B01L';const root='/www/sites/api.cnwenhui.cn/index';
async function php(code:string){const{stdout}=await execFileAsync('docker',['exec','-w',root,container,'php','-r',code],{encoding:'utf8'});return JSON.parse(stdout.trim().split(/\r?\n/).pop()??'{}')as Record<string,unknown>;}
export function generateP11OperationId(){return`OP-E2E-P11-${Date.now().toString(36)}${Math.random().toString(36).slice(2,6)}`;}
export function setupP11Operation(operationId:string){return php(`require 'vendor/autoload.php';require 'tests/support/OperationScheduleFixture.php';(new think\\App())->initialize();echo json_encode(tests\\support\\OperationScheduleFixture::setup(${JSON.stringify(operationId)},['PATIENT_NAME'=>'P11浏览器合成患者']),JSON_UNESCAPED_UNICODE);`);}
export function cleanupP11Operation(operationId:string){return php(`require 'vendor/autoload.php';require 'tests/support/OperationScheduleFixture.php';(new think\\App())->initialize();think\\facade\\Db::connect('samis')->table('anes_quality_defect')->where('operation_id',${JSON.stringify(operationId)})->delete();$r=tests\\support\\OperationScheduleFixture::cleanup(${JSON.stringify(operationId)});echo json_encode(['status'=>$r['visible']?'present':'absent'],JSON_UNESCAPED_UNICODE);`);}
export function statusP11Operation(operationId:string){return php(`require 'vendor/autoload.php';require 'tests/support/OperationScheduleFixture.php';(new think\\App())->initialize();$r=tests\\support\\OperationScheduleFixture::status(${JSON.stringify(operationId)});echo json_encode(['status'=>$r['visible']?'present':'absent'],JSON_UNESCAPED_UNICODE);`);}
